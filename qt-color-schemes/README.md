## Qt Color Scheme

These are color scheme files written for Qt5 and Qt6. To use them, you have to install the `qt5ct` or `qt6ct` application.

Once installed, paste the `.conf` file inside the directory `/home/$USER/.config/qt6ct/colors/super-dark.conf`

Now, open the Qt5ct application and then select this as the **color scheme**.

